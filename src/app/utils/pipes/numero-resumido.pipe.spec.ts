import { NumeroResumidoPipe } from './numero-resumido.pipe';

describe('NumeroResumidoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroResumidoPipe();
    expect(pipe).toBeTruthy();
  });
});
